javascript:(function(){const aiNodes=document.querySelectorAll('[data-subtree="aimc"]');if(aiNodes.length===0){alert('Не удалось найти ответы ИИ.');return;}let chatText="# ЭКСПОРТ ЧАТА GOOGLE AI\n\n";aiNodes.forEach((aiNode,index)=>{let userText="";let turnRoot=aiNode.closest('[data-xid="aim-mars-turn-root"]');if(turnRoot){let userNode=turnRoot.querySelector('[role="heading"][aria-level="2"], [jsname="eFVkfb"]');if(userNode){userText=userNode.innerText.trim();}else{let walker=document.createTreeWalker(turnRoot,NodeFilter.SHOW_TEXT,null,false);let node;while((node=walker.nextNode())){if(aiNode.contains(node))break;let txt=node.nodeValue.trim();if(txt&&!node.parentElement.closest('button, [style*="display: none"]')){userText=txt;break;}}}}if(!userText&&index===0){let searchInput=document.querySelector('textarea[name="q"], input[name="q"]');if(searchInput)userText=searchInput.value;}if(userText){chatText+=`**Пользователь:**\n${userText}\n\n---\n\n`;}let clone=aiNode.cloneNode(true);const noiseSelectors=['button','[role="button"]','[role="dialog"]','[role="alert"]','[role="status"]','svg','img','textarea','input','form','[data-xid*="corroboration"]','[data-xid*="aside"]','[id^="fbproxy"]','ul.aajpme','.ub891','div[aria-live]','[style*="display: none"]','[style*="visibility: hidden"]','[aria-hidden="true"]'].join(', ');clone.querySelectorAll(noiseSelectors).forEach(el=>el.remove());for(let i=1;i<=6;i++){clone.querySelectorAll(`h${i}`).forEach(el=>{el.insertAdjacentText('afterbegin','#'.repeat(i)+' ');el.insertAdjacentText('beforeend','\n\n');});}clone.querySelectorAll('strong, b').forEach(el=>{el.insertAdjacentText('afterbegin','**');el.insertAdjacentText('beforeend','**');});clone.querySelectorAll('em, i').forEach(el=>{el.insertAdjacentText('afterbegin','*');el.insertAdjacentText('beforeend','*');});clone.querySelectorAll('a').forEach(el=>{if(!el.textContent.trim())return;el.insertAdjacentText('afterbegin','[');el.insertAdjacentText('beforeend',`](${el.href})`);});clone.querySelectorAll('pre').forEach(el=>{el.insertAdjacentText('afterbegin','\n```\n');el.insertAdjacentText('beforeend','\n```\n');});clone.querySelectorAll('code').forEach(el=>{if(!el.closest('pre')){el.insertAdjacentText('afterbegin','`');el.insertAdjacentText('beforeend','`');}});clone.querySelectorAll('li').forEach(el=>{let depth=0;let current=el.parentElement;while(current&&current!==clone){if(current.tagName==='UL'||current.tagName==='OL')depth++;current=current.parentElement;}let indent='  '.repeat(Math.max(0,depth-1));let prefix=(el.parentElement&&el.parentElement.tagName==='OL')?'1. ':'- ';el.insertAdjacentText('afterbegin',`${indent}${prefix}`);});clone.querySelectorAll('hr').forEach(el=>el.insertAdjacentText('afterend','\n\n---\n\n'));clone.style.position='absolute';clone.style.left='-9999px';document.body.appendChild(clone);let aiText=clone.innerText.trim();document.body.removeChild(clone);aiText=aiText.replace(/(В ответах искусственного интеллекта могут быть ошибки|Οι απαντήσεις AI μπορεί να περιλαμβάνουν λάθη|AI answers may include mistakes).*/gi,'').replace(/\n{3,}/g,'\n\n').trim();if(aiText){chatText+=`**Google AI:**\n${aiText}\n\n---\n\n`;}});let rawInput=prompt("Напишите название файла как краткое описание диалога:","");let customName=rawInput?rawInput.trim():"Google_AI_Chat";if(customName==="")customName="Google_AI_Chat";customName=customName.replace(/\s+/g,'_').replace(/[\/\\?%*:|"<>]/g,'');let formatInput=prompt("Выберите формат скачивания:\n1 - только .md\n2 - только .txt\n3 - оба файла (.md и .txt)\n\nВведите цифру (по умолчанию 3):","3");let dMd=(formatInput==="1"||formatInput==="3"||!["1","2"].includes(formatInput));let dTxt=(formatInput==="2"||formatInput==="3"||!["1","2"].includes(formatInput));const now=new Date();const time=String(now.getHours()).padStart(2,'0')+'-'+String(now.getMinutes()).padStart(2,'0');const date=String(now.getDate()).padStart(2,'0')+'-'+String(now.getMonth()+1).padStart(2,'0')+'-'+now.getFullYear();const baseFileName=`${customName}_${time}_${date}`;function downloadFile(filename,content){const blob=new Blob([content],{type:'text/plain;charset=utf-8'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=filename;document.body.appendChild(a);a.click();document.body.removeChild(a);URL.revokeObjectURL(a.href);}let savedFiles=[];if(dTxt){downloadFile(`${baseFileName}.txt`,chatText);savedFiles.push(`${baseFileName}.txt`);}if(dMd){downloadFile(`${baseFileName}.md`,chatText);savedFiles.push(`${baseFileName}.md`);}alert(`Чат успешно собран!\nСохранены файлы:\n- ${savedFiles.join('\n- ')}`);})();
